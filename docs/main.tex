\documentclass{article}

\usepackage[final]{neurips_2019}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[colorlinks=true, urlcolor=blue, linkcolor=red, citecolor=blue]{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{nicefrac}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{tabularx}
\usepackage{amsmath}
\usepackage{float}        % for [H] figure placement
\usepackage{enumitem} 
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, calc}

% for subsection spacing
\usepackage{titlesec}
\titlespacing*{\section}{0pt}{-1pt}{-1pt}
\titlespacing*{\subsection}{0pt}{-3pt}{-1pt}
\titlespacing*{\subsubsection}{0pt}{-2pt}{-1pt}

% for figures spacing
\setlength{\textfloatsep}{6pt}
\setlength{\floatsep}{6pt}
\setlength{\intextsep}{6pt}

% for equations spacing
\setlength{\abovedisplayskip}{4pt}
\setlength{\belowdisplayskip}{4pt}
\setlength{\abovedisplayshortskip}{2pt}
\setlength{\belowdisplayshortskip}{2pt}

% for bullets list spacing
\usepackage{enumitem}
\setlist[itemize]{
    noitemsep,
    topsep=0pt,
    partopsep=0pt,
    parsep=0pt
}

\setlist[enumerate]{
    noitemsep,
    topsep=0pt,
    partopsep=0pt,
    parsep=0pt
}
\raggedbottom

\title{
  Predicting TikTok Virality with Late-Fusion Multimodal Learning \\
  \vspace{1em}
  \small{\normalfont Stanford CS229 Project}
}

\author{
  Oscar Rodriguez \\
  Stanford University \\
  \texttt{oscarrm@stanford.edu} \\
  \And
  Mahda Soltani \\
  Stanford University \\
  \texttt{soltanim@stanford.edu} \\
  \And
  Ela Naz Sigin \\
  Stanford University \\
  \texttt{elasigin@stanford.edu}
}

\begin{document}

\maketitle
\vspace{-19pt}

\begin{abstract}
\vspace{-10pt}
Social media platforms like TikTok have become one of the dominant channels
through which information, entertainment, and political and cultural trends
propagate at scale. A defining feature of these platforms is the extreme
skewness in attention: while the vast majority of uploaded videos receive little
to no exposure, a small fraction achieve \textit{viral} reach, accumulating
orders of magnitude more views within a short time window. This heavy-tailed
distribution of engagement naturally raises the question of how to
operationalize and predict ``virality'' in a principled way.

This investigation explores the use of Machine Learning techniques to pin down a
formal framework for the task of predicting the likelihood of a TikTok video
going viral using both intrinsic (e.g.\ video content) and extrinsic features
(e.g.\ video author).
\end{abstract}
\vspace{-11pt}
% =============================================================================
\section{Introduction}
\label{sec:intro}
Social media platforms such as TikTok have become central channels through which information, entertainment, and cultural trends spread at massive scale. A defining feature of these platforms is the extreme inequality in attention: while most uploaded videos receive minimal engagement, a small fraction rapidly accumulates millions of views. This highly skewed distribution raises an important question: can we predict which content is likely to go viral?

In this project, we develop a machine learning framework to estimate the probability that a TikTok video becomes viral by leveraging both intrinsic signals (features derived from the video content itself) and extrinsic signals (information about the creator and posting context). Using a multimodal architecture that integrates text, video, and tabular metadata, we model engagement dynamics and classify videos that fall into the top tail of the engagement distribution.

Our results provide insight into the extent to which measurable platform signals can explain virality, while highlighting the persistent role of stochastic and external factors in shaping online attention.
% =============================================================================
\section{Methods}
\label{sec:methods}

\subsection{Data collection and processing}
\label{sec:data}

We started with a base dataset called TikTok-10M~\cite{tiktok10m} from which we sourced a rich subset of examples that we complemented with a scraping step to download the videos and publisher accounts to extract additional signals (e.g.\ video bytes, aspect ratio) as well as account information (e.g.\ account statistics, publishing history). With all this raw data at hand, we produced a curated dataset~\cite{viral_dataset} for our task. Table~\ref{tab:dataset} documents the dataset dimensions.

\begin{table}[h]
\caption{Dataset dimensions: intrinsic content signals and extrinsic author/metadata signals.}
\label{tab:dataset}
\centering
\small
\begin{tabularx}{\textwidth}{@{}l l X l@{}}
\toprule
\textbf{Dimension} & \textbf{Type} & \textbf{Description} & \textbf{Extraction} \\
\midrule
\multicolumn{4}{@{}l}{\textbf{User / author signals}} \\
user\_id/username & ID/String & Unique identifier and handle of the creator & Scraped \\
user\_verified & Boolean & Verification status of the author & Scraped \\
is\_private & Boolean & Whether the account is set to private & Scraped \\
author\_stats & Floats & Follower, hearts, video, and friend counts & Scraped; $\log(1{+}x)$ \\
\midrule
\multicolumn{4}{@{}l}{\textbf{Video content \& metadata}} \\
description & String & User-generated caption and hashtags & Base dataset \\
temporal\_features & Float & Hour of day and day of week & Sine/cosine encoding \\
duration & Integer & Total length of the video in seconds & Base dataset \\
challenges & String & Associated hashtag challenges or trends & Base dataset \\
video\_bytes & Binary & Scraped video resized to 256$\times$256 @ 1fps & \texttt{yt-dlp} + \texttt{moviepy} \\
detected\_objects & String & Objects detected in video frames & DETR-ResNet-50 \\
\midrule
\multicolumn{4}{@{}l}{\textbf{Audio \& technical specs}} \\
music\_info & String & Title, album, and author of the audio & \texttt{yt-dlp} metadata \\
resolution & Integer & Video width and height in pixels & \texttt{yt-dlp} \\
aspect\_ratio & Float32 & Ratio of width to height & Calculated \\
vq\_score & Float64 & Visual quality score & Base dataset \\
\midrule
\multicolumn{4}{@{}l}{\textbf{Engagement stats \& target labels}} \\
engagement\_score & Float64 & Weighted sum of interactions per view & Engineered \\
view\_velocity & Float64 & Views accumulated per hour since creation & $\log(1{+}x)$ scaled \\
is\_viral & Binary & Classification ($P{=}0.95$) of engagement & Quantile threshold \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Task definition}
\label{sec:task}

The definition of virality (\texttt{is\_viral}) we produced is the target
dimension we optimize for. It is meant to capture outliers relative to any given
account stack-ranked in absolute terms with the goal of producing a signal
that's useful for any account size. We compute two scores for each video:
\textbf{engagement} and \textbf{view velocity}.

\textbf{Engagement}
\begin{equation}
\text{engagement}_i = \frac{\text{share\_count} \times w_0 + \text{save\_count} \times w_1 + \text{comment\_count} \times w_2 + \text{like\_count} \times w_3}{\text{play\_count}}
\label{eq:engagement}
\end{equation}

Engagement represents the volume of "interactions" a video had relative to its
play count, making the score relative to the account size. As shown in
Figure~\ref{fig:play_count}, accounts with a larger audience will most likely
outperform their counterparts in absolute measurements. This signal instead
captures a degree of engagement normalized to audience size: a small account with
a few hundred followers that generates thousands of interactions would have a
comparable engagement to an account with millions of followers that accumulated
hundreds of thousands of interactions.

\begin{figure}[h]
    \centering
    \begin{minipage}{0.47\textwidth}
        \centering
        \includegraphics[width=\textwidth]{assets/play_count_by_account_size_raw.png}
        \caption{Raw play count by account size. Larger accounts naturally
        accumulate more views, motivating our normalized engagement signal.}
        \label{fig:play_count}
    \end{minipage}
    \hfill
    \begin{minipage}{0.47\textwidth}
        \centering
        \includegraphics[width=\textwidth]{assets/view_velocity_by_delta_hours_log.png}
        \caption{View velocity by exposure time. Videos accumulate views most
        rapidly in the first few hours, with velocity dropping sharply thereafter.}
        \label{fig:view_velocity}
    \end{minipage}
\end{figure}

\subsection*{View velocity}
View velocity normalizes play count by the total time a video has been live
(Figure~\ref{fig:view_velocity}). A video with high view velocity accumulates interactions faster:
\vspace{-6pt}
\begin{equation}
\text{view velocity}_i = \frac{\text{play\_count}}{\text{delta\_hours}}
\label{eq:velocity}
\end{equation}

\subsection*{Producing the signal}
We combine both measurements into a signal $S_i$ for each video and consider the
top $5\%$ as ``viral'':
\begin{align}
S_i &= \frac{\text{engagement}_i}{\text{engagement}_{max}} + \frac{\text{view velocity}_i}{\text{view velocity}_{max}} \label{eq:signal} \\
y_{\text{is\_viral}}^{(i)} &=
\begin{cases}
  1 & \text{if } S_i \geq F_S^{-1}(0.95) \\
  0 & \text{otherwise}
\end{cases} \label{eq:viral}
\end{align}

Our reasoning for this definition of virality is rooted in convenience and
usefulness. There are too many dynamic forces that influence virality, like current social
discourse, political events, or sheer luck, which makes it near impossible to
capture all contributing factors. Instead, we condense a signal that is useful
even for small accounts: rolling out a video that is a couple of folds more viral
relative to their expected baseline is considered a win.

\subsection{Model architecture}
\label{sec:architecture}

The architecture implements a mixture of processing techniques for different data
modalities; all branches are concatenated and passed through a late fusion module
to produce 2 regression heads (engagement and view velocity) and 1
classification head (virality). The outputs are stitched together into a final
weighted loss. Figure~\ref{fig:architecture} shows the full pipeline.

\begin{figure}[h]
\centering
\resizebox{\textwidth}{!}{
\begin{tikzpicture}[
    node distance=1.0cm and 1.2cm,
    base_style/.style={rectangle, draw, rounded corners, thick, align=center, inner sep=8pt},
    block/.style={base_style, fill=white, text width=2.8cm, minimum height=1.0cm},
    input/.style={base_style, fill=gray!10, text width=2.8cm, minimum height=1.2cm},
    branch/.style={base_style, fill=blue!5, text width=3.0cm, minimum height=1.2cm},
    head/.style={base_style, fill=orange!5, text width=3.0cm, minimum height=1.2cm},
    loss/.style={base_style, fill=red!5, text width=3.6cm, minimum height=1.4cm},
    arrow/.style={-Stealth, thick}
]
\node[input] (text_in) {\textbf{Text input}\\ \small \texttt{input\_ids} \\ \small (batch, 256)};
\node[input, below=of text_in] (video_in) {\textbf{Video input}\\ \small \texttt{pixel\_values} \\ \small (batch, 16, 3, 224, 224)};
\node[input, below=of video_in] (tab_in) {\textbf{Tabular input}\\ \small \texttt{tabular\_features} \\ \small (batch, 19)};
\node[branch, right=of text_in] (text_mod) {\textbf{Text model}\\ \small Frozen DistilBERT \\ \small (hidden: 768)};
\node[branch, right=of video_in] (video_mod) {\textbf{Video model}\\ \small Frozen VideoMAE \\ \small (hidden: 768)};
\node[branch, right=of tab_in] (tab_mod) {\textbf{Tabular MLP}\\ \small 2-layer MLP \\ \small ($d$=512)};
\node[block, right=1.0cm of video_mod, fill=green!5, text width=3.2cm] (fusion) {\textbf{Late fusion}\\ \small (dim=2048)};
\node[head, right=1.2cm of fusion] (class_head) {\textbf{Classification}\\ \small Virality logit};
\node[head, above=0.4cm of class_head] (eng_head) {\textbf{Engagement}\\ \small Regression logit};
\node[head, below=0.4cm of class_head] (vel_head) {\textbf{Velocity}\\ \small Regression logit};
\node[loss, right=1.4cm of class_head] (total_loss) {\textbf{Multi-task loss}\\
    \scriptsize $\mathcal{L} = \lambda_{\text{reg}}\mathcal{L}_{\text{Huber}} + \lambda_{\text{cls}}\mathcal{L}_{\text{cls}}$ \\
    \small $\lambda_{\text{reg}}{=}0.3,\;\lambda_{\text{cls}}{=}0.7$};
\draw[arrow] (text_in) -- (text_mod);
\draw[arrow] (video_in) -- (video_mod);
\draw[arrow] (tab_in) -- (tab_mod);
\draw[arrow] (text_mod.east) -- ++(0.4,0) |- (fusion.west);
\draw[arrow] (video_mod.east) -- (fusion.west);
\draw[arrow] (tab_mod.east) -- ++(0.4,0) |- (fusion.west);
\draw[arrow] (fusion.east) -- ++(0.4,0) |- (eng_head.west);
\draw[arrow] (fusion.east) -- (class_head.west);
\draw[arrow] (fusion.east) -- ++(0.4,0) |- (vel_head.west);
\draw[arrow, dashed, gray] (eng_head.east) -- ++(0.3,0) |- (total_loss.160);
\draw[arrow, dashed, gray] (vel_head.east) -- ++(0.3,0) |- (total_loss.200);
\draw[arrow, dashed, gray] (class_head.east) -- (total_loss.west);
\end{tikzpicture}
}
\caption{End-to-end architecture: features are extracted from multimodal inputs,
fused into a shared latent space, and passed to specialized heads. The final
objective is a weighted combination of Huber regression loss and classification loss.}
\label{fig:architecture}
\end{figure}

\paragraph{Text input}
The text input is a concatenation of textual dimensions:
\texttt{``[CLS] [TITLE] \dots\ [MUSIC] \dots\ [CHALLENGES] \dots\ [OBJECTS] \dots\ [LOC] \dots''}
fed into a pretrained, frozen DistilBERT~\cite{sanh2019distilbert} backbone to extract a 768-dimensional semantic representation from the [CLS] token.

\paragraph{Video input}
Videos were downsized to 224$\times$224 pixels at 1 FPS. We sample 16 frames and feed them into a frozen VideoMAE~\cite{tong2022videomae} model, then apply global average pooling across the output tokens to yield a 768-dimensional embedding capturing visual features and temporal dynamics.

\textbf{Tabular input}
The tabular branch processes 19 heterogeneous features including author signals,
temporal metadata, and video quality scores. We apply $\log(1{+}x)$ scaling to
high-variance author statistics and sine/cosine cyclic encoding to temporal
features. These are fed into a 2-layer MLP with layer normalization and dropout,
projecting them into a learnable 512-dimensional space.

\textbf{Late fusion}
We concatenate the 768-dim text embedding, 768-dim video embedding, and 512-dim
tabular output into a single 2048-dimensional vector, passed through a shared
fusion MLP mapping to $d_{\text{model}}{=}512$.

\textbf{Multi-head targets}
We employ a multi-head architecture: two regression heads predict the continuous
engagement and view velocity scores, and one classification head predicts
virality (the 95th percentile of the combined distribution).

\paragraph{Loss targets}                                                                                    We experimented with two loss formulations. \textbf{Weighted BCE:} Huber loss~\cite{huber1992robust} for regression and                                                                                                                                                                                                                                              
  weighted BCE for classification, with $\lambda_{\text{cls}}{=}0.7$ and
  $\lambda_{\text{reg}}{=}0.3$; the minority class is upweighted by
  $\frac{p}{1-p}$ where $p{=}0.95$.
  \textbf{Focal loss}~\cite{lin2017focal}\textbf{:} replaces weighted BCE with an
  adaptive mechanism that down-weights easy negatives, with $\gamma{=}2.0$ and
  $\alpha{=}p_{\text{virality\_threshold}}$.

% =============================================================================
\section{Experiments and Results}
\label{sec:experiments}

We track ROC-AUC (general class separability), PR-AUC (our primary metric, as it
does not reward correct prediction of the non-viral majority), and F1-Score at
the 0.5 threshold.

\subsection{Training dynamics}
\label{sec:training}

We compared two training regimes: a multi-task \textbf{weighted loss} baseline
with linear learning rate decay over 15 epochs, and a \textbf{focal loss}
experiment with cosine scheduler and 10\% warmup phase. As shown in
Figure~\ref{fig:training}, both models exhibited steady convergence. The focal
loss model reached optimal performance earlier (around epoch 8), suggesting it
could ignore easy negatives and converge faster on the viral decision boundary.

\begin{figure}[h]
    \centering
    \begin{minipage}{0.47\textwidth}
        \centering
        \includegraphics[width=\textwidth]{assets/loss.png}
    \end{minipage}
    \hfill
    \begin{minipage}{0.47\textwidth}
        \centering
        \includegraphics[width=\textwidth]{assets/focal_loss.png}
    \end{minipage}
    \caption{Training dynamics for weighted loss (left) and focal loss (right).}
    \label{fig:training}
\end{figure}

\subsection{Analysis of results}
\label{sec:results}

\begin{table}[h]
\caption{Comparative performance between weighted BCE and focal loss targets.}
\label{tab:results}
\centering
\begin{tabular}{@{}lccc@{}}
\toprule
\textbf{Metric} & \textbf{Weighted loss} & \textbf{Focal loss} & \textbf{$\Delta$} \\ \midrule
ROC-AUC         & 0.7875                & \textbf{0.8012}    & +1.7\% \\
PR-AUC          & \textbf{0.1805}       & 0.1788             & -0.94\% \\
F1-Score        & 0.2452                & \textbf{0.2620}    & +6.8\% \\
Precision       & 0.2092                & \textbf{0.2479}    & +18.4\% \\
Recall          & \textbf{0.2963}       & 0.2777             & -4.5\% \\ \bottomrule
\end{tabular}
\end{table}

The transition to focal loss yielded a \textbf{18.4\% improvement in precision}.
While the baseline weighted BCE model achieved higher recall by aggressively
upweighting the minority class, it suffered from a high false-positive rate.
Focal loss mitigated this by forcing the model to focus on ``hard'' examples,
resulting in a more balanced F1 of 0.2620.

Despite the marginal improvement in ROC-AUC, PR-AUC stayed mostly across
both experiments, confirming that the absolute confidence in viral classification
is likely bounded by factors we did not model:

\begin{enumerate}
    \item \textbf{Unmeasurable stochasticity:} Too many ongoing dynamics (socio-political discourse, randomness) influence popularity in ways a finite dataset cannot capture.
    \item \textbf{Gaps in dataset:} Missing input sources like audio features, platform-specific nuances (e.g.\ current trends), or richer video dynamics.
\end{enumerate}

\subsection{Feature importance}                                                                             \label{sec:feature_importance}                                                                            
  We run permutation importance on the 19 tabular features (shuffling each across 300 test examples and measuring F1 drop) through two lenses: the \textbf{classification head} and the \textbf{regression heads}
  (Figure~\ref{fig:permutation}).

  \begin{figure}[h]
      \centering
      \begin{minipage}{0.47\textwidth}
          \centering
          \includegraphics[width=\textwidth]{assets/permutation_importance.png}
      \end{minipage}
      \hfill
      \begin{minipage}{0.47\textwidth}
          \centering
          \includegraphics[width=\textwidth]{assets/permutation_importance_regression.png}
      \end{minipage}
      \caption{Permutation importance via classification head (left) and regression heads (right).}
      \label{fig:permutation}
  \end{figure}

  The two heads reveal complementary strategies. The classification head relies
  almost entirely on \textbf{creator reputation}: \texttt{follower\_count} (+0.41),
  \texttt{heart\_count} (+0.23), and \texttt{friend\_count} (+0.14) dominate, while
  content features have near-zero impact. The regression heads instead prioritize
  \textbf{content and temporal properties}: \texttt{duration} (+0.13),
  \texttt{sin\_hour} (+0.10), \texttt{height} (+0.09), and \texttt{vq\_score}
  (+0.08), with creator signals contributing moderately. This suggests the
  classification head learns a shortcut that large accounts are likely to go
  viral, while the regression heads, tasked with predicting continuous scores,
  must attend to finer-grained signals that influence \textit{how much} engagement
  a video receives.
\subsection{Ablation studies}
\label{sec:ablation}
To determine the specific contributions of each modality to our multimodal model, we conduct ablation experiments. Specifically, we zero out one modality's representation at a time using forward hooks and measure metric degradation on 300 test examples (Figure~\ref{fig:ablation}).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{assets/ablation_study.png}
    \caption{Ablation study: metrics when each modality is individually zeroed.
    Removing tabular features causes the most severe degradation (F1 drops to 0).}
    \label{fig:ablation}
\end{figure}


    From Figure 6, we see that Tabular Features are critical, and Text has the smallest individual contribution. Zeroing the tabular MLP output collapses F1 to 0.000, meaning no example's viral probability exceeds the 0.5 threshold without creator and metadata signals. The model does retain some ranking ability (ROC-AUC = 0.831) from text and video alone. Moreover, Video provides moderate signal: removing video reduces F1 from 0.514 to 0.462 and PR-AUC from 0.466 to 0.374.
    Zeroing text causes only a slight F1 drop (0.514 $\to$ 0.444). Interestingly, ROC-AUC \textit{increases} marginally (0.931 $\to$ 0.936), suggesting the text branch may introduce slight noise.


% =============================================================================
\section{Conclusion and Future Work}
\label{sec:conclusion}

While the model provides a strong baseline for late-fusion multimodal learning,
the gap between ROC-AUC and PR-AUC indicates difficulty handling the extreme
sparsity of viral signals. Future work could incorporate additional modalities
such as \textbf{audio features} and richer video dynamics, and explore
\textbf{Self-Exciting Hawkes Process} models to capture engagement growth over time.

Overall, intrinsic content and creator signals provide predictive value for
virality, but outcomes remain highly stochastic. Our multi-task framework
stabilizes training of a high-dimensional multimodal model and offers a useful
foundation for future work on social media dynamics.

% =============================================================================
\section*{Contributions}

\begin{itemize}
    \item \textbf{Oscar Rodriguez:} Implementation of scraping scripts and dataset compilation as well as support with model architecture design and training runs. I also helped putting together the final write up.
    \item \textbf{Mahda Soltani:} Implementation of Temporal CV and assistance with feature importance. I also helped putting together the final write up.
    \item \textbf{Ela Naz Sigin:} Conducted model analysis (ablation studies, feature importance), generating plots and figures for the report. Also experimented with various dataset rebalancing techniques (in practice, training on the rebalanced dataset yielded worse model performance than the original).
\end{itemize}

% =============================================================================
\bibliographystyle{acl_natbib}
\bibliography{references}

% =============================================================================
\newpage
\appendix
\section{Appendix}
\label{sec:appendix}

\subsection{Inference examples}
We ran a few inferences through our final model to understand our model's strengths and limitations. We decided to prioritize recall over precision while fine tuning our model hyperparameters. Our reasoning was that we aim to capture as many viral videos as possible (true positives), and in this application, a false positive does not have drastic consequences (unlike a medical diagnosis application for instance). Additionally, by prioritizing recall, we can get more useful information out of our model (recognizing more of the truly viral videos).
\label{sec:inference}
\begin{figure}[H]
    \centering
    \label{fig:inference}
    \vspace{4pt}
    \includegraphics[width=1.1\textwidth]{assets/more_inference_examples_cropped.png}
    \caption{Inference examples showing model strengths (true
    positives/negatives, green) and shortcomings (false positives/negatives, red).}
\end{figure}

\end{document}
